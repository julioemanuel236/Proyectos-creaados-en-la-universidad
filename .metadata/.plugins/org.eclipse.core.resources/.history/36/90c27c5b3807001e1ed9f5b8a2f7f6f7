package questions;

public class Q26 {
	public class MyData implements Comparable{

		@Override
		public int compareTo(Object o) {
			// TODO Auto-generated method stub
			return 0;
		}
		
	}
	
	private MyData[] myList = null;
	private int numberOfElements = 0;
	
	public int recBinarySearch(int pTarget,int bottom,int top) {
		if(bottom == top) {
			if(myList[bottom].compareTo(pTarget))return bottom;
			else return -1;
		}
		int m = (top+bottom)/2;
		
		if(myList[m].compareTo(pTarget)==0) return m;
		if(pTarget.compareTo(myList[m] == -1)
	}
}
